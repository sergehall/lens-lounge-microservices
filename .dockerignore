# Yarn Plug'n'Play — cache and transient files can be ignored
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# DO NOT ignore these — they are critical for PnP to work
!.pnp.cjs
!.pnp.loader.mjs
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
!.yarn/patches

# ───────────────────────────────────────────
# GIT & SECRETS
# ───────────────────────────────────────────
.git

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ⛔️ .env files are ignored for production — use docker-compose `env_file` instead
.env
.env.*

# ───────────────────────────────────────────
# TESTS & BUILD ARTIFACTS
# ───────────────────────────────────────────
coverage
.nyc_output

# ───────────────────────────────────────────
# IDE / SYSTEM / MISC
# ───────────────────────────────────────────
.vscode
.idea
*.sublime-workspace
.DS_Store

# ───────────────────────────────────────────
# 🐳 Docker: ignore if you don't COPY them explicitly
# ───────────────────────────────────────────
Dockerfile*
docker-compose*.yml
