# Node modules (handled inside container)
**/node_modules

# Yarn Plug'n'Play — cache и временные файлы можно игнорировать...
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

#  ...НО: не игнорируй critical PnP-файлы и плагины
!.pnp.cjs
!.pnp.loader.mjs
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
!.yarn/patches

# ✅ Git
.git

# ✅ Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (handled via Docker secrets or compose env vars)
.env
.env.*

# Test & coverage
coverage
/.nyc_output

# Build output
build
dist

# IDE/editor
.vscode
.idea
*.sublime-workspace

# OS
.DS_Store

# Dockerfiles: IGNORE ONLY IF YOU DON'T COPY THEM IN CONTEXT
# Если ты НЕ копируешь Dockerfile внутрь контейнера — это ОК:
Dockerfile*
docker-compose*.yml
